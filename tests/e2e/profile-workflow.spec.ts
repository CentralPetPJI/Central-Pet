import { expect, test } from "@playwright/test";

test.describe("profile workflow", () => {
  test("desativar conta desativa pets do usuario", async () => {
    // TODO: Fazer remoção em cascata
    expect.any(true);
  });
});
